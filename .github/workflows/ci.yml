name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
      MYSQL_USER: ${{ secrets.MYSQL_USER }}
      MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
      MYSQL_HOST: ${{ secrets.MYSQL_HOST }}
      MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
      DATABASE_URL: 'mysql://\${{ secrets.MYSQL_USER }}:${{ secrets.MYSQL_PASSWORD }}@${{ secrets.MYSQL_HOST }}/${{ secrets.MYSQL_DATABASE }}'

    steps:
      - uses: actions/checkout@v2

      - name: Read .nvmrc
        run: echo "NODE_VERSION=$(cat .nvmrc)"

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Yarn globally
        run: corepack enable

      - name: Install dependencies using Yarn
        run: yarn install

      - name: Run linting using Yarn
        run: yarn run lint

      - name: Run build using Yarn
        run: yarn run clean-build

      - name: Run tests using Yarn
        run: yarn run test
