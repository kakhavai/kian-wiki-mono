{
  "name": "common-types",
  "version": "1.0.0",
  "description": "",
  "main": "./build/index.js",
  "scripts": {
    "build": "yarn run clean && yarn tsc",
    "build:prod": "yarn run clean && yarn tsc",
    "clean": "rimraf ./build",
    "lint": "eslint ./"
  },
  "author": "kakhavai",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.54.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "rimraf": "^5.0.5"
  }
}
