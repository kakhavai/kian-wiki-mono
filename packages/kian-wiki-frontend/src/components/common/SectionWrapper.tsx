import styled, { IStyledComponent, Runtime } from 'styled-components';

export const SectionWrapper: IStyledComponent<Runtime> = styled.div`
  display: flex;
  flex-direction: column;
  color: #ffffffdf;
`;
